rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Grant full read and write access to all authenticated users.
      // This is a permissive rule for development and will be refined later.
      allow read, write: if request.auth != null;
    }
  }
}

    