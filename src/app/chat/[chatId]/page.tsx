export default function ChatIdPage({ params }: { params: { chatId: string } }) {
  // This will be replaced with actual chat fetching from Firebase
  return <div>Chat with ID: {params.chatId}</div>;
}
